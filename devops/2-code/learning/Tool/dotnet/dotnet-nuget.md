# dotnet-nuget

- 新增nuget來源
```dotnetcli
dotnet nuget add
```

- 從伺服器刪除或取消套件
```dotnetcli
dotnet nuget delete
```

- 停用nuget 來源
```dotnetcli
dotnet nuget disable
```

- 啟用nuget來源
```dotnetcli
dotnet nuget enable
```

- 列出nuget來源
```dotnetcli
dotnet nuget list
```
